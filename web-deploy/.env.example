# VarLens Concept Pilot — operator secrets (Layer 2)
#
# Copy to web-deploy/.env (chmod 600) and fill in. This file is the
# single place an operator types their bring-up secrets. `pilot.sh`
# sources it before preflight so values are available across shells
# without re-exporting every session. Shell exports still override —
# `export GHCR_TOKEN=...` in the running shell beats whatever this file
# says, so CI / one-off runs keep working unchanged.
#
# Layer split (see .planning/web/phase2-followup-operator-env.md):
#   - web-deploy/.env             (this file) — operator-typed secrets
#   - web-deploy/compose/.env     (server-only) — runtime config docker
#                                                 compose consumes
#   - web-deploy/tofu/.../tfvars  — Hetzner API token + ssh_pubkey
#                                   (different lifecycle, stays separate)

# --- GHCR (private varlens-web image pull) ------------------------------------
# GitHub PAT (classic) with read:packages scope. Piped to `docker login`
# stdin on the server during stack-up; never written to disk on the server.
GHCR_TOKEN=
# GitHub username that owns the GHCR package; defaults to "robspan".
GHCR_USER=

# --- Hetzner Object Storage (restic backup target) ----------------------------
# REQUIRED. Hetzner does NOT expose an API for S3 credential creation
# (probed 2026-05-07: /v1/object_storage/credentials returns 404). The
# keypair is generated once per account in the console and pasted here.
# Bucket creation and restic-repo setup are automated downstream — this
# is the only manual step in the bring-up.
#
# Generate ONCE: Hetzner Console > Security > S3 Credentials > Generate
# (the secret is shown only at generation time; capture both immediately).
RESTIC_S3_ACCESS_KEY=
RESTIC_S3_SECRET_KEY=

# --- Stack identity (prod vs dev vs siblings) ---------------------------------
# Parameterises docker service+container names, Caddy upstream, and the
# URL prefix. Same IaC stack can host multiple distinct deployments by
# supplying different values per server.
#
#   APP_NAME          example: varlens (prod) / varlens-dev (dev)
#   APP_PATH_PREFIX   URL prefix incl. leading slash; "" = root mount
#   APP_PORT          internal HTTP port the app listens on
#
# IMPORTANT — coupled value: the SPA bundle in the docker image is built
# at image-bake time with vite `base: '/varlens/'` baked in. Changing
# APP_PATH_PREFIX away from /varlens means the SPA's asset URLs stop
# resolving — only the API endpoints will work. For prod/dev separation
# on SEPARATE servers, leave APP_PATH_PREFIX at /varlens on both. For
# co-located deployments with distinct prefixes, the renderer build
# needs to template `base` from this env (TODO).
#
# Defaults match Stage-1 pilot behaviour — leave blank to inherit them.
APP_NAME=
APP_PATH_PREFIX=
APP_PORT=

# --- Restic repository password (optional override) ---------------------------
# If set, used instead of auto-generation by setup-backup.py. The value
# is persisted SOPS-encrypted in secrets/restic.yaml so a cold-start on
# a fresh server can still decrypt the bucket. Leave blank to let
# setup-backup mint a random one (the default).
#
# IMPORTANT: if the bucket already contains snapshots, this MUST match
# what was used when those snapshots were created — otherwise restic
# cannot decrypt them. Changing the value mid-life = orphaning prior
# snapshots.
RESTIC_PASSWORD=

# --- PostgreSQL password (optional override) ----------------------------------
# If set on the FIRST stack-up against a fresh server, this value is
# persisted to /etc/varlens/postgres-password and used instead of an
# auto-generated random one. Subsequent runs reuse the persisted value
# regardless. Leave blank to auto-generate.
POSTGRES_PASSWORD=

# --- VarLens admin bootstrap (one-shot, first-boot only) ----------------------
# Hash-preferred. On the FIRST stack-up against a fresh data volume,
# VarLens creates the admin row from whichever credential field is
# set. The bootstrapped row carries must_change_password=TRUE, so the
# first login forces rotation before any application surface is
# reachable — the bootstrap credential has zero exposure window.
#
# Preferred: VARLENS_ADMIN_PASSWORD_HASH
#   1. Generate the hash locally:
#        npm run varlens:hash-password
#      The plaintext stays in your terminal for a few seconds; the
#      Argon2id PHC string goes to stdout.
#   2. Paste the hash below.
#   3. Leave VARLENS_ADMIN_PASSWORD blank.
#
# Deprecated (kept one migration release): VARLENS_ADMIN_PASSWORD
#   Plaintext travels through this file and the server's .env. The
#   server emits a deprecation warn at boot. Switch to the HASH var.
#
# After the first login (which forces a rotation), blank whichever
# field you used so the bootstrap value isn't carried into the next
# pilot bring-up.
VARLENS_ADMIN_USERNAME=
VARLENS_ADMIN_PASSWORD_HASH=
VARLENS_ADMIN_PASSWORD=
VARLENS_ADMIN_DISPLAY_NAME=
